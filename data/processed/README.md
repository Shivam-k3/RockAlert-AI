# Processed Data Directory

This directory stores processed features:

- `vision_features.npy` - Computer vision features
- `sensor_features.npy` - Sensor statistical features  
- `combined_features.npy` - Combined feature matrix
- `feature_metadata.json` - Feature descriptions

Processed data files are excluded from Git (see .gitignore) but the directory structure is preserved.